import React from "react";

const Header: React.FC = () => {
    return (
        <header style={{ background: "#f4f4f4", padding: "10px 20px", borderBottom: "1px solid #ddd" }}>
            <h1 style={{ margin: 0 }}>Admin Panel</h1>
        </header>
    );
};

export default Header;
